package org.firstinspires.ftc.teamcode.pedroPathing;

/**
 * This enum defines the different modes of operation for the robot, which can be used to determine
 * how the robot should behave in different situations. The AUTO mode is typically used for
 * autonomous routines, where the robot follows a predefined path without user input. The TELEOP
 * mode is used for teleoperated control, where the driver has direct control over the robot's
 * movements and actions. By using this enum, we can easily switch between different modes of
 * operation and implement specific behaviors for each mode in our code.
 */
public enum Mode {AUTO, TELEOP}
